_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
vendor/
.bundle/
.DS_Store

# Not committed: a lockfile generated on this arm64/x86_64-mixed Mac pins
# Darwin platforms and can break the Linux CI build. CI resolves from Gemfile.
Gemfile.lock

# Outreach contact details: personal email addresses, not for a public repo.
_outreach/contacts.local.json

# Signed release forms carry speakers' signatures; this repo is public.
# See _speakers/README.md before changing this.
_speakers/**/releases/

# Generated by bin/speaker-dashboard; regenerate rather than commit.
_outreach/dashboard.html

# clasp (tools/questions-board). .clasprc.json holds OAuth credentials;
# `clasp login --creds` writes one into the working directory.
.clasprc.json
node_modules/
# Only the script id, but this repo is public and the script fronts the grade
# sheet. Copy .clasp.json.example and fill it in locally.
tools/questions-board/.clasp.json
